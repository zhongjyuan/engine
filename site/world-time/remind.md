config.json
```json
{
    "country": "英国",          // 国家(国旗图片存放在images目录,文件名与之对应)
    "region": "格林威治",       // 地区
    "timezone": "0",           // 时区(UTC)
    "position": "left",        // 定位(左边展示/右边展示)
    "description": "UTC/GMT"   // 描述信息(鼠标悬停)
}
```